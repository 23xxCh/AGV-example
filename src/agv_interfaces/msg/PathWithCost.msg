# ============================================================
# PathWithCost.msg - 带代价的路径消息
# ============================================================
#
# 【这是什么】
# 全局路径规划器计算出的路径，除了包含路径点坐标外，
# 还附带每个路径点的"代价"（cost），表示经过该点的难易程度。
#
# 【技术要点】
# - nav_msgs/Path 是ROS2标准的路径消息，只包含一系列位姿(PoseStamped)
# - 我们扩展了它，额外携带代价信息，这在多车调度时很有用
# - 代价越高说明该区域越难通行（障碍物附近、其他AGV占用等）
# - 代价数组与路径点数组一一对应：costs[i] 是 path.poses[i] 的代价
#
# 【为什么需要代价信息】
# - 多车调度时，调度器需要知道路径各段代价来分配不同路线
# - 代价可用于判断路径是否安全（某段代价过高应重新规划）
# - 可视化界面可以用颜色显示代价热力图

std_msgs/Header header

# 规划出的路径（标准ROS2 Path消息，包含一系列位姿点）
nav_msgs/Path path

# 路径上每个点的代价值，与 path.poses 一一对应
# 代价范围：0~254（0=完全自由，254=几乎不可通行，255=致命障碍）
float64[] costs

# 路径总代价（所有点代价的加权和）
float64 total_cost

# 规划耗时（毫秒），用于性能监控
float64 planning_time_ms
