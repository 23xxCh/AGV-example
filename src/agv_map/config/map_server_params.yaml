# ============================================================
# map_server_params.yaml - 地图服务器参数配置
# ============================================================
# ROS2节点参数通过YAML文件传递，格式为：
#   节点名:
#     ros__parameters:
#       参数名: 参数值

map_server:
  ros__parameters:
    # 地图YAML配置文件路径
    # 启动时可通过命令行覆盖：-p yaml_path:=/path/to/map.yaml
    yaml_path: ""
